{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm exec --yes --package=pnpm@10.15.0 -- pnpm run typecheck:libs && PORT=22201 BASE_PATH=/ npm exec --yes --package=pnpm@10.15.0 -- pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm exec --yes --package=pnpm@10.15.0 -- pnpm run typecheck:libs && npm exec --yes --package=pnpm@10.15.0 -- pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
